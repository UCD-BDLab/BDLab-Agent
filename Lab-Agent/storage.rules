rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Emulator-friendly: require auth; loosen to 'true' if you just want it to work.
      allow read, write: if request.auth != null;
    }
  }
}
